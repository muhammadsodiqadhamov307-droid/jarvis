{
  "name": "jarvis-ai",
  "version": "1.0.0",
  "private": true,
  "description": "A JARVIS-inspired AI assistant web application with voice, memory, notes, and productivity tools.",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "postinstall": "npm run install:all",
    "build": "npm run build --prefix frontend",
    "start:app": "powershell -ExecutionPolicy Bypass -File deploy\\windows\\launcher\\start-jarvis.ps1",
    "build:installer": "powershell -ExecutionPolicy Bypass -File deploy\\windows\\installer\\build_installer.ps1",
    "build:agent-installer": "powershell -ExecutionPolicy Bypass -File deploy\\windows\\agent-installer\\build_agent_installer.ps1",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
